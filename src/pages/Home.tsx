import Typography from "~/components/Typography";

const Home = () => {
    return (
        <div>
            <Typography variant="title1">Home</Typography>
        </div>
    );
};

export default Home;
